package Task2;

public class Human extends Animal{
    static Integer count;
    public Human (Integer run, Integer jamp, String name, String sex, Integer stopBit) {
        super(run, jamp, name, sex, stopBit);
       }
}
