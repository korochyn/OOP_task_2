package Task2;

public class Cat extends Animal {
    static Integer countt ;

    public Cat (Integer run, Integer jamp, String name, String sex, Integer stopBit) {
        super(run, jamp, name, sex, stopBit);
        
    }


         
}
